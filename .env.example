# Flask Google Groups Auth - Example Environment Configuration
#
# Copy this file to .env and fill in your actual values
# Never commit .env file to version control!

# ============================================================================
# CONFIGURATION APPROACH
# ============================================================================
# Choose ONE of the following configuration approaches:
#   1. ENVIRONMENT VARIABLES (recommended for local development)
#   2. SECRET MANAGER (recommended for production/Cloud Run)
#
# The library automatically tries Secret Manager if an environment variable
# is not set. Secret Manager uses Application Default Credentials (ADC) to
# determine the project automatically.
#
# To use Secret Manager:
# - Comment out the Local Development Environment variables listed below
# - Ensure your application has access to Secret Manager (via ADC)
# - Create secrets in Secret Manager with the SAME NAMES as the environment variables
# - Example: create a secret named "DELEGATED_ADMIN_EMAIL" containing admin@yourdomain.com

# ============================================================================
# ENVIRONMENT VARIABLES (Local Development)
# ============================================================================

SERVICE_ACCOUNT_KEY_FILE=/path/to/service-account-key.json
DELEGATED_ADMIN_EMAIL=admin@yourdomain.com
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret
REDIRECT_URI=http://localhost:8080/auth/callback
SECRET_KEY=your-secret-key-here

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Cache TTL in seconds (default: 3600 = 1 hour)
# FLASK_GOOGLE_GROUPS_AUTH_CACHE_TTL=3600

